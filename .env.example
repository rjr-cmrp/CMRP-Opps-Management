# .env.example
# Copy this file to .env and fill in your actual secrets (never commit .env to git)

# JWT secret for signing tokens
JWT_SECRET=your-very-secret-key

# PostgreSQL database credentials
PGUSER=reuelrivera
PGHOST=localhost
PGDATABASE=opps_management
PGPASSWORD=''
PGPORT=5432
