# .env.example
# Copy this file to .env and fill in your actual secrets (never commit .env to git)

# Database Configuration (Neon PostgreSQL)
DATABASE_URL=postgresql://opps_management_owner:npg_Br9RoWqlTPZ0@ep-quiet-dawn-a1jwkxgx-pooler.ap-southeast-1.aws.neon.tech/opps_management?sslmode=require

# JWT secret for signing tokens
JWT_SECRET=your-very-secret-key

# Environment
NODE_ENV=development

# Port (will be set automatically by Render in production)
PORT=3000

# Frontend URL (for CORS - will be your frontend URL in production)
FRONTEND_URL=http://localhost:3000
